/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com'], // We'll use Unsplash for professional images
  },
}

module.exports = nextConfig 